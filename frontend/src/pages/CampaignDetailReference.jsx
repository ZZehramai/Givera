import { useEffect, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { CheckCircle2, CircleDollarSign, FileImage, Heart, ImagePlus, LockKeyhole, Paperclip, Send, Share2, Sparkles, Trash2, Upload, X } from "lucide-react";
import api from "../api/axios";
import { mediaUrl } from "../utils/mediaUrl";

const fallbackImage = "https://images.unsplash.com/photo-1488521787991-ed7bbaae773c?auto=format&fit=crop&w=1600&q=80";
const kyat = (value) => `${Number(value || 0).toLocaleString()} Ks`;

export default function CampaignDetailReference() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [loadedAt] = useState(() => Date.now());
  const [campaign, setCampaign] = useState(null); const [donors, setDonors] = useState([]); const [updates, setUpdates] = useState([]); const [reports, setReports] = useState([]);
  const [activeCover, setActiveCover] = useState("");
  const [amount, setAmount] = useState(""); const [provider, setProvider] = useState("kbzpay"); const [message, setMessage] = useState(""); const [anonymous, setAnonymous] = useState(false); const [error, setError] = useState(""); const [checkout, setCheckout] = useState(null); const [receipt, setReceipt] = useState(null); const [busy, setBusy] = useState(false);
  const [updateTitle, setUpdateTitle] = useState(""); const [updateBody, setUpdateBody] = useState(""); const [updateMedia, setUpdateMedia] = useState([]); const [updateError, setUpdateError] = useState(""); const [updateBusy, setUpdateBusy] = useState(false);
  const [galleryFiles, setGalleryFiles] = useState([]); const [galleryCaption, setGalleryCaption] = useState(""); const [galleryError, setGalleryError] = useState(""); const [galleryBusy, setGalleryBusy] = useState(false); const [galleryInputKey, setGalleryInputKey] = useState(0);
  const [reportForm, setReportForm] = useState({ title: "", description: "", amount_spent: "", spent_on: "", evidence: null }); const [reportError, setReportError] = useState("");
  const user = JSON.parse(localStorage.getItem("user") || "null"); const isAdmin = user?.role === "admin" || user?.is_staff; const isOrganizer = user?.id === campaign?.owner; const loggedIn = Boolean(localStorage.getItem("access"));
  const load = async () => { const [a, b, c, d] = await Promise.all([api.get(`/campaigns/${id}/`), api.get(`/campaigns/${id}/donors/`), api.get(`/campaigns/${id}/updates/`), api.get(`/campaigns/${id}/fund-utilization/`)]); setCampaign(a.data); setActiveCover(a.data.cover_image || a.data.cover_media?.[0]?.file || ""); setDonors(b.data); setUpdates(c.data); setReports(d.data); setReportForm((current) => ({ ...current, amount_spent: current.amount_spent || (Number(a.data.amount_raised) > 0 ? String(a.data.amount_raised) : "") })); };
  // This request intentionally refreshes whenever the route campaign id changes.
  // eslint-disable-next-line react-hooks/set-state-in-effect, react-hooks/exhaustive-deps
  useEffect(() => { load().catch(() => setError("This campaign could not be found.")); }, [id]);
  const startPayment = async (event) => { event.preventDefault(); setError(""); if (Number(amount) < 1000) return setError("Enter at least 1,000 Ks."); setBusy(true); try { const { data } = await api.post("/donations/demo-checkout/", { campaign_id: id, provider, amount: Number(amount), message, is_anonymous: anonymous }); setCheckout(data); } catch { setError("Could not start the demo payment."); } finally { setBusy(false); } };
  const completePayment = async () => { setBusy(true); try { const { data } = await api.post(`/donations/demo-checkout/${checkout.id}/simulate/`, { outcome: "success" }); setCheckout(null); setReceipt(data); setAmount(""); setMessage(""); setAnonymous(false); await load(); } catch { setError("Payment simulation could not be completed."); } finally { setBusy(false); } };
  const publishUpdate = async (event) => { event.preventDefault(); if (!updateTitle.trim() || !updateBody.trim()) return setUpdateError("Add a title and an update message."); setUpdateBusy(true); setUpdateError(""); const form = new FormData(); form.append("title", updateTitle.trim()); form.append("body", updateBody.trim()); updateMedia.forEach((file) => form.append("media", file)); try { const { data } = await api.post(`/campaigns/${id}/updates/`, form); setUpdates((items) => [data, ...items]); setUpdateTitle(""); setUpdateBody(""); setUpdateMedia([]); } catch (requestError) { setUpdateError(requestError.response?.data?.media?.[0] || requestError.response?.data?.detail || "Could not publish the update."); } finally { setUpdateBusy(false); } };
  const uploadGalleryMedia = async (event) => { event.preventDefault(); if (!galleryFiles.length) return setGalleryError("Choose at least one photo or video."); setGalleryBusy(true); setGalleryError(""); const form = new FormData(); galleryFiles.forEach((file) => form.append("files", file)); if (galleryCaption.trim()) form.append("caption", galleryCaption.trim()); try { const { data } = await api.post(`/campaigns/${id}/media/`, form); setCampaign((current) => ({ ...current, gallery_media: [...data, ...(current.gallery_media || [])] })); setGalleryFiles([]); setGalleryCaption(""); setGalleryInputKey((value) => value + 1); } catch (requestError) { setGalleryError(requestError.response?.data?.files?.[0] || requestError.response?.data?.detail || "Could not upload the selected media."); } finally { setGalleryBusy(false); } };
  const removeGalleryMedia = async (mediaId) => { if (!window.confirm("Remove this item from the campaign gallery?")) return; try { await api.delete(`/campaigns/${id}/media/${mediaId}/`); setCampaign((current) => ({ ...current, gallery_media: (current.gallery_media || []).filter((item) => item.id !== mediaId) })); } catch { setGalleryError("Could not remove this media item."); } };
  const publishReport = async (event) => { event.preventDefault(); setReportError(""); if (!reportForm.title || !reportForm.description || !reportForm.amount_spent || !reportForm.spent_on) return setReportError("Complete all required report details."); const form = new FormData(); Object.entries(reportForm).forEach(([key, value]) => { if (value) form.append(key, value); }); try { const { data } = await api.post(`/campaigns/${id}/fund-utilization/`, form); setReports((items) => [data, ...items]); setReportForm({ title: "", description: "", amount_spent: Number(campaign.amount_raised) > 0 ? String(campaign.amount_raised) : "", spent_on: "", evidence: null }); } catch { setReportError("Could not publish the spending report."); } };
  if (error && !campaign) return <div className="grid min-h-screen place-items-center bg-[#F7F5FC]"><p className="font-bold text-slate-700">{error}</p></div>;
  if (!campaign) return <div className="grid min-h-screen place-items-center bg-[#F7F5FC]"><div className="h-9 w-9 animate-spin rounded-full border-4 border-[#DDD3FF] border-t-[#6F52D9]" /></div>;
  const progress = Math.min(Number(campaign.progress_percentage || 0), 100); const days = Math.max(0, Math.ceil((new Date(campaign.deadline) - loadedAt) / 86400000));
  return <div className="min-h-screen bg-[#F7F5FC] pb-16 text-[#201A36]"><main className="mx-auto max-w-[1280px] px-5 py-8 sm:px-8"><button type="button" onClick={() => navigate(-1)} className="mb-5 inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-[#6F52D9]">← Back</button><div className="grid gap-x-7 lg:grid-cols-[minmax(0,1fr)_292px]"><section><span className="inline-flex items-center gap-1 rounded-full bg-[#FFD66B] px-3 py-1 text-[11px] font-extrabold text-[#4C3910]"><Sparkles size={13} /> {campaign.status === "completed" ? "Completed Campaign" : "Featured Campaign"}</span><h1 className="mt-3 max-w-3xl text-4xl font-extrabold leading-[1.05] tracking-tight sm:text-5xl">{campaign.title}</h1><p className="mt-4 max-w-3xl text-[15px] leading-6 text-slate-600">{campaign.summary}</p><img src={mediaUrl(activeCover, fallbackImage)} alt="" className="mt-5 aspect-[16/9] w-full rounded-xl object-cover shadow-lg shadow-slate-300/40" /><CoverChoices campaign={campaign} activeCover={activeCover} setActiveCover={setActiveCover} /><CampaignGallery items={campaign.gallery_media || []} reports={reports} isOrganizer={isOrganizer} files={galleryFiles} setFiles={setGalleryFiles} caption={galleryCaption} setCaption={setGalleryCaption} inputKey={galleryInputKey} busy={galleryBusy} error={galleryError} onUpload={uploadGalleryMedia} onRemove={removeGalleryMedia} /><section className="mt-10"><h2 className="text-2xl font-extrabold">The Mission</h2><p className="mt-4 whitespace-pre-wrap text-sm leading-7 text-slate-700">{campaign.story}</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><Impact icon={<CircleDollarSign size={23} />} title="Transparent giving" body="Every donation and administrator-issued spending report is visible here." /><Impact icon={<CheckCircle2 size={23} />} title="Verified campaign" body={`Supporting ${campaign.beneficiary} in ${campaign.location}.`} /></div></section><section className="mt-10"><h2 className="text-2xl font-extrabold">Milestones & Progress</h2><div className="mt-6 border-l-2 border-dashed border-[#D9CEFF] pl-5"><Milestone state="Completed" title="Campaign approved" body="Givera verified this campaign for the community." /><Milestone state="Active phase" title="Fundraising and community support" body="Donations are currently helping this campaign reach its goal." /><Milestone state="Upcoming" title="Fund utilization reports" body="Administrators publish evidence after funds are used." /></div></section><CampaignUpdates updates={updates} isOrganizer={isOrganizer} title={updateTitle} setTitle={setUpdateTitle} body={updateBody} setBody={setUpdateBody} media={updateMedia} setMedia={setUpdateMedia} busy={updateBusy} error={updateError} onSubmit={publishUpdate} /><section className="mt-10"><h2 className="text-2xl font-extrabold">How the funds are used</h2><p className="mt-2 text-sm text-slate-600">Reports below are published by Givera administrators for full transparency.</p>{isAdmin && <AdminReportForm form={reportForm} setForm={setReportForm} onSubmit={publishReport} error={reportError} />}{reports.length ? <div className="mt-5 space-y-3">{reports.map((report) => <article key={report.id} className="rounded-xl bg-white p-5 shadow-sm"><h3 className="font-extrabold">{report.title}</h3><p className="mt-1 text-xs font-bold text-[#6F52D9]">{kyat(report.amount_spent)} · {new Date(report.spent_on).toLocaleDateString()}</p><p className="mt-3 text-sm leading-6 text-slate-600">{report.description}</p>{report.evidence && <a href={mediaUrl(report.evidence)} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-2 text-sm font-bold text-[#6F52D9]"><FileImage size={16} /> View evidence</a>}</article>)}</div> : <p className="mt-5 rounded-xl bg-white p-5 text-sm text-slate-500">No reports published yet.</p>}</section></section><aside className="mt-7 space-y-6 lg:mt-[190px]"><DonationCard campaign={campaign} progress={progress} days={days} donors={donors} loggedIn={loggedIn} amount={amount} setAmount={setAmount} provider={provider} setProvider={setProvider} message={message} setMessage={setMessage} anonymous={anonymous} setAnonymous={setAnonymous} error={error} busy={busy} onSubmit={startPayment} /><DonorCard donors={donors} /><div className="rounded-xl bg-[#EEE9FF] p-5"><p className="text-xs font-bold uppercase tracking-widest text-slate-500">Organizer</p><div className="mt-4 flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded bg-[#6F52D9] font-extrabold text-white">{campaign.owner_name?.[0]?.toUpperCase()}</div><div><p className="text-sm font-extrabold">{campaign.owner_name}</p><p className="text-xs text-slate-600">Campaign organizer</p></div></div></div></aside></div></main>{checkout && <Checkout checkout={checkout} busy={busy} onClose={() => setCheckout(null)} onComplete={completePayment} />}{receipt && <Receipt receipt={receipt} onClose={() => setReceipt(null)} />}</div>;
}

function CoverChoices({ campaign, activeCover, setActiveCover }) {
  const covers = [campaign.cover_image, ...(campaign.cover_media || []).map((item) => item.file)].filter(Boolean);
  if (covers.length < 2) return null;
  return <div className="mt-3 flex gap-2 overflow-x-auto pb-1" aria-label="Campaign cover images">
    {covers.map((cover, index) => <button key={cover} type="button" onClick={() => setActiveCover(cover)} className={`shrink-0 overflow-hidden rounded-lg border-2 ${activeCover === cover ? "border-[#6F52D9]" : "border-transparent opacity-70 hover:opacity-100"}`} aria-label={`Show cover image ${index + 1}`}><img src={mediaUrl(cover)} alt="" className="h-16 w-24 object-cover" /></button>)}
  </div>;
}

function CampaignGallery({ items, reports, isOrganizer, files, setFiles, caption, setCaption, inputKey, busy, error, onUpload, onRemove }) {
  const evidence = reports.filter((report) => report.evidence);
  const hasContent = items.length > 0 || evidence.length > 0;

  return <section className="mt-10">
    <div className="flex flex-wrap items-end justify-between gap-3">
      <div><p className="text-xs font-extrabold uppercase tracking-[0.16em] text-[#6F52D9]">Photos, videos & evidence</p><h2 className="mt-1 text-2xl font-extrabold">Campaign gallery</h2><p className="mt-2 text-sm text-slate-600">Follow the campaign through organizer media and administrator-verified report evidence.</p></div>
      {hasContent && <span className="rounded-full bg-[#EEE9FF] px-3 py-1 text-xs font-extrabold text-[#6549C9]">{items.length + evidence.length} items</span>}
    </div>
    {hasContent ? <div className="mt-5 grid gap-3 sm:grid-cols-2">
      {items.map((item) => <MediaTile key={item.id} item={item} canRemove={isOrganizer} onRemove={onRemove} />)}
      {evidence.map((report) => <EvidenceTile key={`evidence-${report.id}`} report={report} />)}
    </div> : <div className="mt-5 rounded-xl border border-dashed border-[#CFC4F7] bg-white/60 px-5 py-8 text-center"><ImagePlus className="mx-auto text-[#8066DE]" size={28} /><p className="mt-3 text-sm font-extrabold">No gallery media yet</p><p className="mt-1 text-xs text-slate-500">Campaign photos, videos, and report evidence will appear here.</p></div>}
    {isOrganizer && <form onSubmit={onUpload} className="mt-4 rounded-xl border border-[#DDD3FF] bg-[#F1EDFF] p-5">
      <div className="flex items-start gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-lg bg-white text-[#6F52D9]"><Upload size={19} /></span><div><p className="text-sm font-extrabold">Add to the gallery</p><p className="mt-1 text-xs leading-5 text-slate-500">Select up to 6 photos or videos. Each file can be up to 25 MB.</p></div></div>
      <div className="mt-4 grid gap-3 sm:grid-cols-[1fr_auto]"><div><input key={inputKey} type="file" multiple accept="image/jpeg,image/png,image/gif,image/webp,video/mp4,video/webm,video/quicktime" onChange={(event) => setFiles(Array.from(event.target.files || []).slice(0, 6))} className="block w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs text-slate-600 file:mr-3 file:rounded-md file:border-0 file:bg-[#E9E3FF] file:px-3 file:py-1.5 file:font-bold file:text-[#6549C9]" />{files.length > 0 && <p className="mt-1 text-[11px] font-bold text-[#6549C9]">{files.length} file{files.length === 1 ? "" : "s"} selected</p>}</div><button disabled={busy} className="rounded-lg bg-[#6F52D9] px-5 py-2 text-sm font-extrabold text-white disabled:opacity-60">{busy ? "Uploading…" : "Upload media"}</button></div>
      <input value={caption} onChange={(event) => setCaption(event.target.value)} maxLength={200} placeholder="Optional caption for these files" className="mt-3 w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm outline-none focus:border-[#6F52D9]" />
      {error && <p className="mt-2 text-xs font-bold text-rose-600">{error}</p>}
    </form>}
  </section>;
}

function MediaTile({ item, canRemove, onRemove }) {
  return <article className="group relative overflow-hidden rounded-xl bg-slate-900 shadow-sm">
    {item.media_type === "video" ? <video controls preload="metadata" src={mediaUrl(item.file)} className="aspect-video w-full object-cover" /> : <a href={mediaUrl(item.file)} target="_blank" rel="noreferrer"><img src={mediaUrl(item.file)} alt={item.caption || "Campaign gallery"} className="aspect-video w-full object-cover transition duration-300 group-hover:scale-[1.02]" /></a>}
    {(item.caption || canRemove) && <div className="absolute inset-x-0 bottom-0 flex items-end justify-between gap-3 bg-gradient-to-t from-slate-950/90 to-transparent px-4 pb-3 pt-10 text-white"><p className="line-clamp-2 text-xs font-semibold">{item.caption || (item.media_type === "video" ? "Campaign video" : "Campaign photo")}</p>{canRemove && <button type="button" onClick={() => onRemove(item.id)} aria-label="Remove media" className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-white/15 hover:bg-rose-500"><Trash2 size={15} /></button>}</div>}
  </article>;
}

function EvidenceTile({ report }) {
  const url = mediaUrl(report.evidence);
  const cleanUrl = url.split("?")[0].toLowerCase();
  const isImage = /\.(jpe?g|png|gif|webp)$/.test(cleanUrl);
  const isVideo = /\.(mp4|webm|mov|m4v)$/.test(cleanUrl);
  return <article className="overflow-hidden rounded-xl border border-[#F1D780] bg-[#FFFAE9]">
    {isImage ? <a href={url} target="_blank" rel="noreferrer"><img src={url} alt={`${report.title} evidence`} className="aspect-video w-full object-cover" /></a> : isVideo ? <video controls preload="metadata" src={url} className="aspect-video w-full bg-slate-900 object-cover" /> : <a href={url} target="_blank" rel="noreferrer" className="grid aspect-video place-items-center bg-[#FFF4C7] text-[#755A08]"><FileImage size={34} /><span className="sr-only">Open evidence</span></a>}
    <div className="flex items-center justify-between gap-3 px-4 py-3"><div><p className="text-[10px] font-extrabold uppercase tracking-wider text-[#8A6A08]">Verified report evidence</p><p className="mt-1 text-sm font-extrabold">{report.title}</p></div><a href={url} target="_blank" rel="noreferrer" className="shrink-0 text-xs font-extrabold text-[#6549C9]">Open</a></div>
  </article>;
}

function CampaignUpdates({ updates, isOrganizer, title, setTitle, body, setBody, media, setMedia, busy, error, onSubmit }) {
  return <section className="mt-10"><div className="flex items-end justify-between gap-3"><div><p className="text-xs font-extrabold uppercase tracking-[0.16em] text-[#6F52D9]">From the organizer</p><h2 className="mt-1 text-2xl font-extrabold">Latest Updates</h2></div>{updates.length > 0 && <span className="text-xs font-bold text-slate-400">{updates.length} published</span>}</div>
    {isOrganizer && <form onSubmit={onSubmit} className="mt-5 rounded-xl border border-[#DDD3FF] bg-white p-5 shadow-sm"><p className="text-sm font-extrabold">Share campaign progress</p><p className="mt-1 text-xs text-slate-500">Keep supporters informed and attach up to 6 photos or videos as evidence.</p><input value={title} onChange={(event) => setTitle(event.target.value)} maxLength={160} placeholder="Update title" className="mt-4 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-[#6F52D9]" /><textarea value={body} onChange={(event) => setBody(event.target.value)} maxLength={2000} rows={4} placeholder="What progress has been made?" className="mt-3 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-[#6F52D9]" /><label className="mt-3 flex cursor-pointer items-center gap-2 rounded-lg border border-dashed border-[#CFC4F7] bg-[#F8F6FF] px-3 py-3 text-xs font-bold text-[#6549C9]"><Paperclip size={16} /> Add photos or videos<input type="file" multiple accept="image/jpeg,image/png,image/gif,image/webp,video/mp4,video/webm,video/quicktime" onChange={(event) => setMedia(Array.from(event.target.files || []).slice(0, 6))} className="sr-only" /></label>{media.length > 0 && <div className="mt-2 flex flex-wrap gap-2">{media.map((file) => <span key={`${file.name}-${file.size}`} className="rounded-full bg-[#EEE9FF] px-3 py-1 text-[11px] font-bold text-[#6549C9]">{file.name}</span>)}</div>}{error && <p className="mt-2 text-xs font-bold text-rose-600">{error}</p>}<button disabled={busy} className="mt-3 inline-flex items-center gap-2 rounded-lg bg-[#6F52D9] px-4 py-2.5 text-sm font-extrabold text-white disabled:opacity-60"><Send size={15} /> {busy ? "Publishing…" : "Publish update"}</button></form>}
    {updates.length ? <div className="mt-5 space-y-4">{updates.map((update) => <article key={update.id} className="rounded-xl bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-2"><p className="text-xs font-bold text-[#6F52D9]">{new Date(update.created_at).toLocaleDateString()}</p><p className="text-[11px] font-bold text-slate-400">{update.author_name}</p></div><h3 className="mt-1 font-extrabold">{update.title}</h3><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-slate-600">{update.body}</p>{update.media?.length > 0 && <div className="mt-4 grid gap-2 sm:grid-cols-2">{update.media.map((item) => <MediaTile key={item.id} item={item} canRemove={false} />)}</div>}</article>)}</div> : !isOrganizer && <p className="mt-5 rounded-xl bg-white p-5 text-sm text-slate-500">The organizer has not published an update yet.</p>}
  </section>;
}

function DonationCard({ campaign, progress, days, donors, loggedIn, amount, setAmount, provider, setProvider, anonymous, setAnonymous, error, busy, onSubmit }) { return <div className="rounded-[26px] border border-slate-200 bg-white p-6 shadow-[0_14px_34px_rgba(32,33,68,.09)] lg:h-[504px]"><p className="text-lg font-extrabold text-slate-900">Support this campaign</p><p className="mt-1 text-sm leading-5 text-slate-500">Your contribution goes directly toward the campaign goal.</p><div className="mt-5 border-y border-slate-100 py-4"><div className="flex items-end justify-between"><div><p className="text-2xl font-extrabold tracking-tight text-[#6549C9]">{kyat(campaign.amount_raised)}</p><p className="mt-1 text-xs font-semibold text-slate-400">raised of {kyat(campaign.goal_amount)}</p></div><p className="text-lg font-extrabold text-slate-700">{progress}%</p></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#6F52D9]" style={{ width: `${progress}%` }} /></div><div className="mt-3 flex justify-between text-xs font-bold text-slate-500"><span>{donors.length} supporters</span><span>{days} days remaining</span></div></div>{campaign.status === "completed" ? <div className="mt-5 rounded-xl bg-[#FFFAE9] p-5 text-center"><CheckCircle2 size={28} className="mx-auto text-[#7A5B00]" /><p className="mt-3 font-extrabold text-slate-800">Campaign completed</p><p className="mt-1 text-xs leading-5 text-slate-500">This campaign has reached its goal or deadline and no longer accepts donations.</p></div> : loggedIn ? <form onSubmit={onSubmit} className="mt-5"><label className="text-sm font-extrabold text-slate-800">Choose an amount</label><div className="mt-3 grid grid-cols-4 gap-2">{[5000, 10000, 20000, 50000].map((preset) => <button key={preset} type="button" onClick={() => setAmount(String(preset))} className={`rounded-lg px-1 py-2 text-xs font-extrabold ${Number(amount) === preset ? "bg-[#EAE6FF] text-[#6549C9]" : "bg-slate-100 text-slate-600 hover:bg-slate-200"}`}>{preset >= 1000 ? `${preset / 1000}k` : preset}</button>)}</div><div className="mt-3 flex rounded-xl border border-slate-200 focus-within:border-[#6F52D9]"><span className="grid w-12 place-items-center border-r border-slate-200 bg-slate-50 text-xs font-extrabold text-[#6549C9]">Ks</span><input type="number" min="1000" step="1000" value={amount} onChange={(e) => setAmount(e.target.value)} placeholder="Or enter another amount" className="min-w-0 flex-1 rounded-r-xl px-3 py-3 text-sm font-bold outline-none" /></div><div className="mt-4"><p className="text-xs font-extrabold uppercase tracking-wide text-slate-400">Pay with</p><div className="mt-2 grid grid-cols-3 gap-2">{[["kbzpay", "KBZPay"], ["wave", "Wave"], ["mmqr", "MMQR"]].map(([key, label]) => <button key={key} type="button" onClick={() => setProvider(key)} className={`rounded-lg border py-2 text-xs font-extrabold ${provider === key ? "border-[#6F52D9] bg-white text-[#6549C9] shadow-sm" : "border-slate-100 bg-slate-50 text-slate-500"}`}>{label}</button>)}</div></div><label className="mt-4 flex items-center gap-2 text-xs font-semibold text-slate-500"><input type="checkbox" checked={anonymous} onChange={(e) => setAnonymous(e.target.checked)} className="accent-[#6F52D9]" /> Donate anonymously</label>{error && <p className="mt-2 text-xs font-bold text-rose-600">{error}</p>}<button disabled={busy} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-[#6F52D9] py-3.5 font-extrabold text-white shadow-lg shadow-violet-200"><Heart size={17} /> {busy ? "Opening checkout…" : "Donate now"}</button></form> : <Link to="/login" className="mt-5 flex items-center justify-center gap-2 rounded-xl bg-[#6F52D9] py-3.5 font-extrabold text-white"><Heart size={17} /> Donate now</Link>}<div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-4"><button type="button" onClick={() => navigator.clipboard?.writeText(window.location.href)} className="inline-flex items-center gap-1.5 text-xs font-bold text-slate-500 hover:text-[#6549C9]"><Share2 size={15} /> Share campaign</button><span className="inline-flex items-center gap-1 text-[11px] font-bold text-slate-400"><LockKeyhole size={12} /> Demo only</span></div></div>; }

function Impact({ icon, title, body }) { return <article className="rounded-lg border border-[#DDD3FF] bg-[#F0ECFF] p-5"><span className="text-[#6F52D9]">{icon}</span><h3 className="mt-3 text-sm font-extrabold">{title}</h3><p className="mt-2 text-xs leading-5 text-slate-700">{body}</p></article>; }
function Milestone({ state, title, body }) { return <div className="relative pb-8 pl-4 last:pb-0"><span className={`absolute -left-[27px] top-0 h-3 w-3 rounded-full ring-4 ${state === "Upcoming" ? "bg-slate-300 ring-slate-100" : state === "Completed" ? "bg-[#6F52D9] ring-[#EEE9FF]" : "bg-[#9B86E8] ring-[#EEE9FF]"}`} /><p className="text-[11px] font-bold uppercase tracking-wide text-[#6F52D9]">{state}</p><p className="mt-1 text-sm font-extrabold">{title}</p><p className="mt-1 text-sm text-slate-600">{body}</p></div>; }
function DonorCard({ donors }) { return <div className="rounded-xl bg-[#F3F0FF] p-5"><div className="flex items-center justify-between"><h2 className="text-lg font-extrabold">Recent Donors</h2><span className="text-xs font-bold text-[#6F52D9]">See all</span></div><div className="mt-4 space-y-4">{donors.slice(0, 4).map((donor) => <div key={donor.id} className="flex items-center justify-between gap-2"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#FFD66B] text-xs font-extrabold text-[#5A460B]">{donor.donor_name?.slice(0, 2).toUpperCase()}</div><div><p className="text-xs font-extrabold">{donor.donor_name}</p><p className="text-[11px] text-slate-500">{new Date(donor.created_at).toLocaleDateString()}</p></div></div><p className="text-xs font-extrabold text-[#6F52D9]">{kyat(donor.amount)}</p></div>)}{!donors.length && <p className="py-3 text-center text-sm text-slate-500">Be the first supporter.</p>}</div><div className="mt-5 rounded-lg bg-[#FFFAE9] px-4 py-4 text-center"><Heart size={20} className="mx-auto text-[#7A5B00]" /><p className="mt-2 text-xs font-extrabold">Join the movement</p><p className="text-[11px] text-slate-600">{donors.length} people have already contributed.</p></div></div>; }
function AdminReportForm({ form, setForm, onSubmit, error }) { return <form onSubmit={onSubmit} className="mt-5 rounded-xl border border-[#DDD3FF] bg-[#F4F1FF] p-5"><p className="text-sm font-extrabold text-[#6549C9]">Admin: publish spending report</p><div className="mt-3 grid gap-2 sm:grid-cols-2"><input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} placeholder="Expense title" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input value={form.amount_spent} onChange={(e) => setForm({ ...form, amount_spent: e.target.value })} type="number" placeholder="Amount Ks" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input value={form.spent_on} onChange={(e) => setForm({ ...form, spent_on: e.target.value })} type="date" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><label className="rounded-lg border border-slate-200 bg-white px-2 py-2 text-xs text-slate-500">Supporting attachment<input onChange={(e) => setForm({ ...form, evidence: e.target.files?.[0] || null })} type="file" className="mt-1 block w-full text-xs" /></label></div><p className="mt-2 text-[11px] text-slate-500">Any file type is accepted, up to 5 MB.</p><textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} rows={3} placeholder="Explain how funds were used" className="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" />{error && <p className="mt-2 text-xs font-bold text-rose-600">{error}</p>}<button className="mt-3 rounded-lg bg-[#6F52D9] px-4 py-2 text-sm font-bold text-white">Publish report</button></form>; }
function Checkout({ checkout, busy, onClose, onComplete }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4"><div className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-xl"><div className="flex items-center justify-between"><p className="text-sm font-extrabold text-slate-900">Demo payment</p><button type="button" onClick={onClose} className="text-slate-400 hover:text-slate-700"><X size={20} /></button></div><div className="mt-5 rounded-xl bg-[#F0ECFF] p-4"><p className="text-xs font-bold uppercase tracking-widest text-[#6F52D9]">{checkout.provider_label}</p><p className="mt-2 text-3xl font-extrabold text-slate-900">{kyat(checkout.amount)}</p><p className="mt-2 font-mono text-xs font-bold text-slate-500">{checkout.transaction_reference}</p></div><button disabled={busy} onClick={onComplete} className="mt-5 w-full rounded-lg bg-[#6F52D9] py-3 font-bold text-white"><CheckCircle2 className="mr-2 inline" size={18} /> {busy ? "Completing…" : "Confirm demo payment"}</button><p className="mt-3 text-center text-xs text-slate-400">No real funds are transferred.</p></div></div>; }
function Receipt({ receipt, onClose }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4"><div className="w-full max-w-sm rounded-2xl bg-white p-7 text-center shadow-2xl"><CheckCircle2 className="mx-auto text-emerald-600" size={42} /><h2 className="mt-4 text-2xl font-extrabold">Donation completed</h2><p className="mt-2 text-sm text-slate-500">{kyat(receipt.amount)} via {receipt.provider_label}</p><p className="mt-3 font-mono text-xs font-bold text-slate-600">{receipt.transaction_reference}</p><button onClick={onClose} className="mt-6 w-full rounded-lg bg-[#6F52D9] py-3 font-bold text-white">Done</button></div></div>; }
